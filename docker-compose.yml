services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    # 휘발성 ON (볼륨 없으면 데이터 날아감)
    # volumes:
    #   - pgdata:/var/lib/postgresql/data
