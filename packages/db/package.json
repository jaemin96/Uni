{
  "name": "@mono/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/migrate.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests for @mono/db\""
  },
  "dependencies": {
    "drizzle-orm": "^0.39.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@mono/ts-config": "workspace:*",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.11",
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}

